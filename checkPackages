#!/bin/sh

export DISPLAY=:0

ping -c 2 1.1.1.1>/dev/null || exit 1

/usr/bin/notify-send " Syncing Repositories"

packages="$(pacman -Qu)"

test -n "$packages" && /usr/bin/notify-send " Updates are available:" "\n$packages" || /usr/bin/notify-send " No updates available"
