#!/bin/sh

export DISPLAY=:0

/home/beta/.bin/checkWifi raw>/dev/null || exit 1

/usr/bin/notify-send " Syncing Repositories"

test -n "$(xbps-install -Mun)" && /usr/bin/notify-send " Updates are available" || /usr/bin/notify-send " No updates available"
